name: Update Minecraft Resources

on:
  # 매주 일요일 00:00 (UTC) 자동 실행
  schedule:
    - cron: '0 0 * * 0'

  # 수동 실행 가능
  workflow_dispatch:

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4 pandas python-dateutil tqdm pydantic

      - name: Run data update script
        run: |
          python parse_html.py || echo "크롤링 실패, 계속 진행..."

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet data/resources.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push if changed
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/resources.json
          git commit -m "🤖 자동 데이터 업데이트 $(date +'%Y-%m-%d')"
          git push

      - name: No changes
        if: steps.changes.outputs.changed == 'false'
        run: |
          echo "ℹ️ 변경사항 없음 - 데이터가 이미 최신입니다."
