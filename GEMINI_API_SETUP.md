# 🤖 Gemini API 설정 가이드

AI 챗봇 기능을 사용하려면 Gemini API 키가 필요합니다.

---

## 1️⃣ Gemini API 키 발급

### Google AI Studio에서 발급

1. **Google AI Studio 접속**
   - 링크: https://makersuite.google.com/app/apikey
   - Google 계정으로 로그인

2. **API 키 생성**
   - "Create API Key" 버튼 클릭
   - 프로젝트 선택 또는 새로 생성
   - API 키 복사 (한 번만 표시됩니다!)

3. **API 키 안전하게 보관**
   - 메모장이나 안전한 곳에 저장
   - 절대 GitHub에 커밋하지 마세요!

---

## 2️⃣ 로컬 개발 환경 설정

### `.streamlit/secrets.toml` 파일 생성

1. **디렉토리 생성**
   ```bash
   mkdir .streamlit
   ```

2. **secrets.toml 파일 생성**
   ```bash
   # Windows
   notepad .streamlit\secrets.toml

   # Mac/Linux
   nano .streamlit/secrets.toml
   ```

3. **API 키 추가**
   ```toml
   GEMINI_API_KEY = "your-api-key-here"
   ```

4. **파일 저장 후 대시보드 실행**
   ```bash
   streamlit run dashboard.py
   ```

---

## 3️⃣ Streamlit Cloud 배포 설정

### Streamlit Cloud에 Secret 추가

1. **Streamlit Cloud 대시보드 접속**
   - https://share.streamlit.io/
   - GitHub 계정으로 로그인

2. **앱 선택**
   - `library-minecraft` 앱 클릭

3. **Settings → Secrets 메뉴**
   - ⚙️ 설정 아이콘 클릭
   - "Secrets" 탭 선택

4. **API 키 추가**
   ```toml
   GEMINI_API_KEY = "your-api-key-here"
   ```

5. **Save 버튼 클릭**
   - 앱이 자동으로 재시작됩니다

---

## 4️⃣ API 키 테스트

### 대시보드에서 확인

1. **대시보드 실행**
   ```bash
   streamlit run dashboard.py
   ```

2. **"🤖 AI 추천" 탭 클릭**

3. **테스트 질문**
   - "초등학생용 코딩 수업 추천해줘"
   - AI가 응답하면 성공! ✅

4. **API 키 오류 시**
   - ⚠️ 경고 메시지가 표시됩니다
   - API 키를 다시 확인하세요

---

## 5️⃣ 주의사항

### 보안

- ❌ **절대 하지 마세요:**
  - API 키를 GitHub에 커밋
  - 공개 채팅/포럼에 공유
  - 코드에 직접 하드코딩

- ✅ **해야 할 것:**
  - `.streamlit/secrets.toml` 사용
  - `.gitignore`에 `secrets.toml` 추가됨 (이미 설정됨)
  - API 키를 안전하게 보관

### 사용 제한

- **무료 티어:**
  - 분당 60회 요청
  - 일일 1,500회 요청
  - 충분히 사용 가능! 🎉

- **초과 시:**
  - 유료 플랜으로 업그레이드 가능
  - 링크: https://ai.google.dev/pricing

---

## 6️⃣ 문제 해결

### "API 키가 설정되지 않았습니다" 오류

1. **secrets.toml 파일 위치 확인**
   ```
   프로젝트루트/
   └── .streamlit/
       └── secrets.toml  ← 여기에 있어야 함
   ```

2. **파일 형식 확인**
   ```toml
   # 올바른 형식
   GEMINI_API_KEY = "AIza..."

   # 잘못된 형식
   GEMINI_API_KEY: "AIza..."  ❌ (콜론 대신 등호)
   GEMINI_API_KEY = AIza...    ❌ (따옴표 없음)
   ```

3. **대시보드 재시작**
   ```bash
   # Ctrl+C로 중지 후 재실행
   streamlit run dashboard.py
   ```

### "API 호출 실패" 오류

1. **API 키 유효성 확인**
   - Google AI Studio에서 키가 활성화되어 있는지 확인

2. **네트워크 연결 확인**
   - 인터넷 연결 상태 확인

3. **사용량 제한 확인**
   - 무료 티어 한도 초과 여부 확인

---

## 7️⃣ 추가 리소스

- **Gemini API 문서:** https://ai.google.dev/docs
- **Streamlit Secrets 문서:** https://docs.streamlit.io/streamlit-community-cloud/deploy-your-app/secrets-management
- **Google AI Studio:** https://makersuite.google.com/

---

문의사항이나 문제가 있으면 GitHub Issues에 등록해주세요!
